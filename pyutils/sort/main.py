from bead import *
from bogo import *
from bubble import *
from cocktail_shaker import *
from heap import *
from insertion import *
from merge import *
from quick import *
from selection import *
from shell import *
from stalin import *
class sort:
    def __init__(self),
        pass
    def bogo_sort(self, array):
        bogo_sort(array)
    def bubble_sort(self, array):
        bubble_sort(array)
    def bead_sort(self, array):
        bead_sort(array)
    def cocktail_shaker_sort(self, array):
        cocktail_shaker_sort(array)
    def heap_sort(self, array):
        heap_sort(array)
    def insertion_sort(self, array):
        insertion_sort(array)
    def merge_sort(self, array):
        merge_sort(array)
    def quick_sort(self, array):
        quick_sort(array)
    def selection_sort(self, array):
        selection_sort(array)
    def shell_sort(self, array):
        shell_sort(array)
    def stalin_sort(self,array):
        stalin_sort(array)